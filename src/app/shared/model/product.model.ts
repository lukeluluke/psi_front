import { Observable} from 'rxjs/Observable';

export class Product {
    id: string;
    sku: string;
    name: string;
    barcode: string;
    spec: string;
    model: string;
    unit: string;
    price: number;
}
